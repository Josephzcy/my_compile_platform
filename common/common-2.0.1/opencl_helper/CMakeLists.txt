AUX_SOURCE_DIRECTORY(. SRC_LIST)

MESSAGE(STATUS ${SRC_LIST})

ADD_LIBRARY(opencl_helper ${LIBS_TYPE} common.cpp image.cpp)
TARGET_LINK_LIBRARIES(opencl_helper)

INCLUDE_DIRECTORIES(${PROJECT_ROOT_PATH})
